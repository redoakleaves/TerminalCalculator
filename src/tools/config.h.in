#ifndef _CONFIG_H_
#define _CONFIG_H_

#include <string>
#include <map>

#include "yaml-cpp/yaml.h"

#define PROJECT_VER "@PROJECT_VERSION@"

namespace Tools
{
    class Config {
    public:
        Config();
        ~Config();

        void load_config();

        std::string get_value(const char* key);

    private:
        char* home_path;

        std::map<std::string, std::string> config_store;

        void parse_recursive(std::string prefix, YAML::Node child);
    };
}

#endif
